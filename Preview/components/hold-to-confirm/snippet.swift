import SwiftUI
import SwiftBits

struct HoldToConfirmButtonExample: View {
    var body: some View {
        HoldToConfirmButton("Hold to confirm") {
            // Perform your action.
        }
    }
}
