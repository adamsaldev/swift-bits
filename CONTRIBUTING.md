# Contributing to SwiftBits

Thanks for contributing. Keep each component focused, configurable, documented,
and accessible.

1. Create a branch from `main`.
2. Add or update tests and DocC documentation.
3. Run `swift build` and `swift test`.
4. Open a pull request describing the behavior and API decisions.

New public APIs should include documentation comments and sensible defaults.
